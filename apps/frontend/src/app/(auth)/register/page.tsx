import { RegisterForm } from "@/components/auth/RegisterForm";

export default function RegisterPage() {
  return (
    <section>
      <h1>Create account</h1>
      <p>Start building and operating workflows.</p>
      <RegisterForm />
    </section>
  );
}
