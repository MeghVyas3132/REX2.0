import { LoginForm } from "@/components/auth/LoginForm";

export default function LoginPage() {
  return (
    <section>
      <h1>Sign in</h1>
      <p>Access your REX workspace.</p>
      <LoginForm />
    </section>
  );
}
